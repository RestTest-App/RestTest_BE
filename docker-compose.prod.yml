
services:
  fastapi:
    build:
      context: .
      args:
        MODE: prod
    container_name: resttest-prod
    image: resttest-server
    command: uvicorn main:app --host 0.0.0.0 --port 8080
    ports:
      - "8080:8080"
    env_file:
      - .env.prod